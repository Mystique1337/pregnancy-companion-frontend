# secrets — never commit
.env
.env.*
!.env.example

# dependencies / build output
node_modules/
.next/
out/
dist/
build/
*.tsbuildinfo
next-env.d.ts

# os / editor
.DS_Store

# installed agent skills (tooling, not app source)
.agents/

# local working files (n8n workflow exports, data dumps)
others/*.json

# Python
__pycache__/

# Playwright
test-results/
playwright-report/
.last-run.json

# Local-only: real values for pasting into the host's env panel
coolify.env

# Local-only demo runbook
DEMO.md
