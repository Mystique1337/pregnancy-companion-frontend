# Pin Node 22 + bundle cloudflared (for the optional Cloudflare Tunnel DB sidecar).
# Install + build are auto-detected for Next.js; start is scripts/start.sh.
[phases.setup]
nixPkgs = ["nodejs_22", "cloudflared"]
